# 邻动项目 1.0 交接说明

更新时间：2026-06-16

## 1. 项目概述

邻动当前不是单一应用，而是一套围绕微信小程序业务搭建的完整交付体系，当前主业务已经收口到“5 节课课包拼团”。

当前正式交付范围包含：

- 家长端微信小程序
- 运营后台 Web 端
- 小程序业务后端 `lindong-api`
- 后台管理接口 `console-api-service`
- MySQL 主数据层
- CloudBase 云托管 / 静态托管部署链路
- 首页定位云函数与定时任务云函数
- 回归测试与上线校验脚本

项目当前最重要的业务链路是：

- 课程/课包浏览
- 开团 / 参团
- 下单与微信原生支付
- 拼团成功 / 失败后的状态推进
- 后台课包、拼团、订单、账号、日志管理

## 2. 当前版本状态

### 2.1 1.0 已完成范围

- 小程序主页面与核心业务流程已落地
- 运营后台主功能已落地
- 小程序后端与后台接口已拆分部署
- MySQL 数据层与主要业务规则已落地
- 微信原生支付主链路已接入
- 退款状态机已落地，支持 `refund_pending / refunded / refund_failed`
- 支付成功回调幂等、并发与最后名额竞争规则已补齐
- 拼团详情页首课时间、剩余时间展示规则已按当前口径落地
- 小程序从“最近使用 / 小程序列表”重新打开统一回首页的入口收口已落地
- 终态团、隐藏团、退款受限查看者的旧分享链接兜底已落地
- 拼团结果订阅通知、通知任务投递链路已接入
- 本地回归、后台 live smoke、生产只读 smoke 已形成基础测试体系

### 2.2 1.0 待完成范围

- 微信小程序备案通过
- 正式环境支付/退款最终联调确认
- 上线前完整回归与业务验收
- 环境参数、账号、部署入口的最终交接确认

### 2.3 建议认定的 1.0 完成标志

建议将以下四项同时满足，作为 1.0 完成标志：

- 备案完成
- 正式微信支付可用
- 正式退款可用
- 核心回归通过

## 3. 当前功能范围

### 3.1 家长端微信小程序

当前已覆盖的主要功能：

- 启动协议确认
- 协议列表与协议详情页
- 微信身份登录
- 首页课程/课包列表展示
- 首页 Banner 展示
- 定位、地址解析、地址搜索、手动切换位置
- 课程/课包详情展示
- 开团页
- 参团支付确认页
- 微信原生支付
- 支付结果页
- 拼团详情页
- 我的拼团列表
- 我的页面
- 分享拼团
- 拼团结果订阅通知
- 小程序最近使用 / 列表重开首页收口
- 终态团旧分享链接回课程详情 / 首页

当前业务规则重点：

- 当前主业务已从早期“单次课程拼团”收口为“5 节课课包拼团”
- 前台文案多使用“课程”，后台与接口仍大量沿用 `package`
- 创建订单未支付前，不计入拼团人数
- 支付成功后，才真正写入拼团成员并更新人数
- 参团支付在团名额临界时，后端必须保证最后一个名额只能成功一笔，未抢到名额的支付成功单需直接进入退款链路
- 退款中间态期间，不提前扣减首页/详情页中的团人数与展示，只有订单最终进入 `refunded` 才真正释放名额
- 若订单或支付记录已进入 `refund_pending / refund_failed / refunded`，后续重复支付成功回调不得再把它写回普通成功态
- 成团当天如果刚好就是用户选择的上课星期，首课时间必须顺延到下一周同一星期和整点，不能“当天成团当天上课”
- 拼团详情页在进行中状态下，需要在上课地点下方展示 `拼团剩余时间`
- 拼团详情页是“单团视角”，不承接课程维度聚合展示
- 从微信“小程序最近使用 / 列表”重新打开时，默认回首页；但分享卡片、订阅通知、支付回跳等明确目标页入口仍保留原落地页
- 用户通过分享链接访问已失败、已取消、已隐藏的团，或当前查看者因退款态被限制查看详情时，前端统一提示“当前拼团已结束，您可另外开团”，并跳该团对应课程详情页；课程不可访问时回首页

### 3.2 运营后台

当前主导航与核心模块：

- 概览
- Banner 管理
- 课包管理
- 课包拼团
- 课包订单
- 账号管理
- 操作日志

后台当前已支持的主要能力：

- 管理员登录与权限控制
- 概览数据统计与异常提示
- Banner 新建、编辑、复制、上下线管理
- 课包新建、编辑、复制、上下架管理
- 查看课包拼团记录与拼团详情
- 已成团拼团安排教练
- 订单列表、详情、手动退款
- 管理员账号管理
- 关键操作日志留痕

### 3.3 后端服务

当前后端承载的核心业务能力：

- 小程序登录鉴权
- 课程/课包列表与详情读取
- 拼团详情读取
- 我的拼团查询
- 订单创建
- 微信支付统一下单参数准备
- 微信支付结果回调处理与幂等保护
- 退款状态推进、退款失败收口、退款终态同步
- 拼团结果订阅通知入队与处理
- 后台管理接口
- 共享业务规则复用

## 4. 核心技术架构

### 4.1 总体架构

当前项目建议按“前台一端、后台一端、两套服务、一套 MySQL、若干辅助云函数/任务”来理解：

- 小程序前端：`miniprogram/`
- 后台前端：`console/`
- 小程序业务后端：`backend/lindong-api/`
- 后台接口后端：`backend/console-api-service/`
- 数据库：MySQL
- 云函数：`ip-geolocation`、`lindong-cron`

其中：

- 小程序通过 `wx.cloud.callContainer` 调用 `lindong-api`
- 后台前端通过 HTTP 调用 `console-api-service`
- 两套服务端共用 `backend/shared` 风格的业务规则与部分领域逻辑
- `wechat-pay`、`wechat-pay-callback` 相关代码仍保留在仓库中，但不再是当前主支付链路

### 4.2 前端技术栈

#### 小程序端

- 微信原生小程序
- `WXML + WXSS + JavaScript`
- `weui-miniprogram`
- `style: v2`
- `glass-easel`
- `lazyCodeLoading`

#### 后台前端

- React 19
- TypeScript
- Vite 6
- React Router 7

### 4.3 后端技术栈

- Node.js
- Express
- JWT
- MySQL
- dotenv
- CloudBase 云托管
- 仓库内保留 Wrangler / Worker 入口，用于部分运行与调试能力

### 4.4 当前关键目录

建议交接时重点认这几个目录：

- `miniprogram/`：微信小程序主工程
- `console/`：运营后台前端
- `backend/`：后端源码与脚本总目录
- `backend/lindong-api/`：小程序正式后端部署目录
- `backend/console-api-service/`：后台接口正式部署目录
- `backend/migrations/`：MySQL migration
- `cloudfunctions/ip-geolocation/`：定位与地址解析云函数
- `cloudfunctions/lindong-cron/`：定时触发内部任务云函数
- `qa/regression/`：Playwright 与小程序逻辑回归
- `docs/`：产品、技术、部署、交接文档

注意：

- 早期独立前端原型目录已移除，不是当前主业务入口
- `docs/console/邻动体适能运营后台/` 是文档型原型工程，不是当前正式后台入口

## 5. 支付、退款与通知链路

### 5.1 当前支付主链路

当前支付链路是“微信原生支付”，不是“支付云函数主导的云调用支付”。

当前主链路为：

1. 小程序创建订单
2. 小程序调用后端 `/api/payments/prepare`
3. 后端按当前 `PAYMENT_PROVIDER_MODE=wechat` 生成统一下单参数
4. 小程序直接调用 `wx.requestPayment()`
5. 微信支付成功后，微信回调 `lindong-api` 的支付回调接口
6. 后端按 `orders` 与 `payment_records` 做幂等落库
7. 后端执行入团、人数更新、成团、退款竞争收口等业务逻辑

### 5.1.1 2026-06 测试环境支付问题现状

2026-06-16 在测试环境联调时，已出现过以下现象：

- 微信支付收银台显示支付成功
- 小程序前端仍停留在“支付中”
- “我的拼团”未查到新开团记录
- `payment_records.status=pending`
- `orders.status=pending`

当前已确认：

- 小程序支付发起链路能走通，说明下单与 `wx.requestPayment` 参数准备基本正常
- 只要 `payment_records` 和 `orders` 同时停留在 `pending`，就说明“支付成功后的状态推进”没有完成
- 该状态推进原本强依赖微信异步回调 `/api/payments/notify/wechat`

本轮已完成的后端补强：

- 在支付状态查询链路增加“微信查单补记成功”兜底
- 即：当前端轮询 `/api/payments/status` 时，如本地仍是 `pending/processing`，后端会按 `out_trade_no` 主动向微信查单
- 若微信侧返回 `SUCCESS/PAID`，后端会复用既有支付成功处理逻辑，补更新 `payment_records`、`orders`、课包团入团和人数状态
- 在 `/api/payments/notify/wechat` 增加了脱敏日志，便于确认“回调未到达”还是“回调到达但校验失败”

这次修复解决的是“异步回调缺失时订单永久卡 pending”的收口问题，但**不等于支付链路已完全排查完毕**。
尤其是“读取链路还没全检查”，后续仍需继续补齐。

### 5.2 当前退款链路

当前退款能力已包含：

- 后台手动退款
- 满员竞争失败后的退款收口
- 退款中间态与终态同步
- 退款失败收口

当前需要特别关注：

- `refund_pending` 期间不要提前释放人数
- 支付回调与退款回调可能并发或先后交错到达
- 重复支付成功回调不能覆盖退款态

### 5.3 订阅通知链路

当前已接入拼团结果订阅通知，相关链路包括：

- 小程序支付结果页 / 拼团详情页发起订阅
- 后端记录订阅状态与通知任务
- `cloudfunctions/lindong-cron` 定时触发内部处理接口
- 后端调用微信订阅消息接口执行投递

### 5.4 仓库内保留但非当前主链路的支付代码

仓库中仍保留：

- `cloudfunctions/wechat-pay/`
- `cloudfunctions/wechat-pay-callback/`
- `backend/lindong-api/routes/payments.js` 中的部分 `cloudpay` 内部路由

这些内容当前更适合视为“历史接入 / 备用能力 / 调试资产”，不要把它们误认为当前正式支付主链路。

## 6. 部署方案

### 6.1 当前推荐部署形态

#### 小程序侧

- 前端载体：微信小程序
- 服务调用：`wx.cloud.callContainer`
- 云托管服务：`lindong-api`
- 正式部署目录：`backend/lindong-api`

#### 后台侧

- 后台前端：CloudBase 静态托管
- 后台接口：CloudBase 云托管
- 后台前端源码目录：`console/`
- 后台接口部署目录：`backend/console-api-service`

#### 辅助能力

- 首页定位与地址搜索：`cloudfunctions/ip-geolocation`
- 定时内部任务：`cloudfunctions/lindong-cron`

### 6.2 部署注意事项

- 小程序后端发布时，必须以 `backend/lindong-api` 为准，不能只改混合源码目录后不同步部署目录
- 后台接口发布时，必须确认 `backend/console-api-service` 已同步最新代码
- 首页定位问题优先检查 `ip-geolocation`
- 订阅通知问题优先检查 `lindong-cron` 与后端内部处理接口
- 后台静态托管必须配置 SPA 路由回退到 `index.html`

### 6.3 建议同步确认的环境信息

- MySQL 实例与账号信息
- CloudBase 环境 ID
- 小程序 AppID
- 云托管服务名
- 后台静态托管站点
- 后台接口访问域名
- 支付相关商户配置
- 订阅通知模板 ID
- 云函数环境变量

## 7. 当前自动化测试与发布要求

### 7.1 自动化测试分层

当前自动化测试主要分为 4 层：

#### 7.1.1 本地 Playwright 页面回归

目录：

- `qa/regression`

执行命令：

- `npm test`

当前覆盖：

- 用户侧 `frontend` 基础页面打开
- 后台 dashboard 展示
- 课包创建、编辑、下架
- 课包详情跳转拼团页 / 订单页
- Banner 查询、创建、编辑、上下线、复制、上传
- 账号创建、编辑、删除
- 订单手动退款流程
- 非超管权限页访问限制

#### 7.1.2 小程序逻辑回归

目录：

- `qa/regression`

执行命令：

- `npm run test:miniprogram`

当前覆盖：

- `app` 启动与入口收口
- 首页页面逻辑
- 我的页面逻辑
- 课包价格计算与地点归一化
- 课程详情页开团/参团登录拦截
- 支付确认页校验与支付结果处理
- 我的拼团列表筛选与分页
- 拼团详情页支付成功态展示与订阅流转

#### 7.1.3 Console live smoke

执行命令：

- `npm run test:console-live`

当前覆盖：

- 后台真实登录
- dashboard
- 课包列表
- 课包拼团
- 课包订单与订单详情
- 账号
- 日志
- 部分可回滚的数据更新验证

#### 7.1.4 生产只读 smoke

执行命令：

- `npm run test:console-prod`

当前定位：

- 生产环境只读冒烟
- 验证已部署后台页面与后台接口是否联通
- 不执行创建、编辑、退款等写操作

### 7.2 后端校验与常用命令

主要命令位于 `backend/package.json`：

- `npm run verify:course-lifecycle`
- `npm run verify:group-rules`
- `npm run verify:admin-seed`
- `npm run verify:console-api-smoke`

### 7.3 每次发布上线必须执行回归

这不是建议，而是发布要求。

最低要求：

- 有代码改动就必须回归
- 有数据库变更就必须回归
- 有支付、订单、拼团、后台配置相关改动就必须做重点回归

推荐命令基线：

- `cd /Users/yun/lindong/backend && npm run verify:group-rules`
- `cd /Users/yun/lindong/backend && npm run verify:admin-seed`
- `cd /Users/yun/lindong/backend && npm run verify:console-api-smoke`
- `cd /Users/yun/lindong/console && npm run lint && npm run build:cloudbase`
- `cd /Users/yun/lindong/qa/regression && npm run test:miniprogram`
- `cd /Users/yun/lindong/qa/regression && npm test`
- `cd /Users/yun/lindong/qa/regression && npm run test:console-live`

若本次改动涉及以下高风险区域，还需要追加专项回归：

- 微信支付
- 退款
- 拼团人数计算
- 订单状态流转
- 拼团最后名额并发竞争
- 首课日期自动计算
- 拼团详情页倒计时展示
- 小程序启动入口路由
- 分享链接落地兜底
- 拼团结果订阅通知
- 后台课包上下架
- Banner 上下线

## 8. 当前关键运行关系

当前运行关系建议理解为：

1. 家长在小程序内访问前台业务
2. 小程序通过 CloudBase 容器调用 `lindong-api`
3. `lindong-api` 访问 MySQL 并处理拼团、订单、登录、支付、通知等业务
4. 支付时，小程序直接走微信原生 `wx.requestPayment`
5. 微信支付结果回调 `lindong-api`
6. 运营人员在浏览器访问后台静态站点
7. 后台前端调用 `console-api-service`
8. `console-api-service` 访问同一套 MySQL 主数据
9. 首页定位与地址搜索依赖 `ip-geolocation`
10. 拼团结果通知定时任务依赖 `lindong-cron`

## 9. 当前高风险点

交接时最容易踩坑的点如下：

- 文案层常说“课程”，但代码与接口大量沿用 `package`
- 仓库里存在历史/备用支付实现，当前正式口径必须认“微信原生支付”
- 小程序主入口是 `miniprogram/`
- 后台主入口是 `console/`，不是 `docs/console/邻动体适能运营后台/`
- 订单创建成功不等于参团成功，只有支付成功才真正入团
- 支付成功后的状态展示不只看写入链路，还要看读取链路；当前读取链路还没全检查，不能默认所有查询口径都已兼容新排课/支付兜底逻辑
- 退款中间态与支付成功回调存在并发竞争，不能只看单表状态
- 最后一个名额的并发支付是核心高风险场景
- 分享链接落地、最近使用重开首页、订阅通知落地页这几类入口容易回归破坏

## 10. 建议优先交接清单

建议正式交接时，优先把下面内容一次性讲清楚：

1. 哪些目录是当前正式入口，哪些只是原型/历史资产
2. 当前支付主链路是“微信原生支付”，不是“支付云函数主链路”
3. 后端正式部署目录与后台接口正式部署目录分别是什么
4. MySQL、CloudBase、支付商户、后台账号、订阅模板等环境资产由谁持有
5. 上线前必须执行哪些验证命令和回归动作
6. 哪些业务规则属于绝不能改坏的红线：人数、退款、最后名额、首课日期、分享兜底、入口收口

## 11. 2026-06 后续任务

以下任务在本次修复后仍未全部完成，交接时必须明确：

1. 继续核查微信支付异步回调真实根因。
说明：当前只补了“查单补记成功”兜底，还没有最终确认测试环境回调失败究竟是“未到达容器”还是“到达后签名/解密/配置失败”。

2. 完整检查支付读取链路。
说明：读取链路还没全检查，至少要继续核查“我的拼团”“拼团详情”“支付结果页”“支付状态轮询返回值”“后台订单详情”这些入口，在支付补记成功后是否都能稳定读到最新状态。

3. 核对测试环境支付配置。
说明：需重点确认 `WX_PAY_NOTIFY_URL`、`WX_PAY_PUBLIC_KEY` / `WX_PAY_PLATFORM_CERT`、`WX_PAY_PUBLIC_KEY_ID` 与测试环境当前生效配置是否一致。

4. 做一轮真实测试环境支付专项回归。
说明：至少覆盖开团支付成功、参团支付成功、取消支付、重复轮询、支付后进入“我的拼团”、拼团详情人数更新、后台订单状态同步。

5. 继续检查课包排课改动的读取链路。
说明：本次排课需求前端交互和主写入链路已做，但读取链路还没全检查，需要继续核查 package group 详情、订单快照、后台展示、列表聚合和历史数据兼容。

6. 评估是否要把“微信查单补记成功”同步到更多状态查询入口。
说明：当前兜底是在支付状态查询口，如果后续发现还有其他关键读取入口绕过该接口，需要决定是否下沉到更公共的支付状态读取层。

以上内容如果交接清楚，接手人就能较快分辨“当前真实链路”“仓库历史资产”和“上线前必须守住的业务红线”。
