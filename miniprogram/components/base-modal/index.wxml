<app-dialog
  visible="{{visible}}"
  title="{{title}}"
  confirmText="{{confirmText}}"
  cancelText="{{cancelText}}"
  showCancel="{{showCancel}}"
  extClass="base-modal-shell"
  bind:close="handleMaskTap"
  bind:cancel="handleCancel"
  bind:confirm="handleConfirm"
>
  <slot></slot>
</app-dialog>
